@model Bubblevel_MatchService.Models.Comment

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Comment</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Text)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Text)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CreatedAt)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CreatedAt)
        </dd>
        <dt class="col-sm-2">
            Customer
        </dt>
        <dd class="col-sm-10">
            @ViewBag.CustomerName
        </dd>
    </dl>
</div>
<div class="mt-4">
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.Id" asp-route-supportId="@ViewBag.SupportIncidentId" asp-route-name="@ViewBag.CustomerName">Edit</a> |
    <a asp-action="Index" asp-route-supportId="@ViewBag.SupportIncidentId" asp-route-name="@ViewBag.CustomerName">Back to List</a>
</div>
